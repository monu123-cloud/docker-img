# Use a basic image as the base (e.g., Ubuntu)
FROM ubuntu:latest
 
# Set the working directory inside the container
WORKDIR /app
 
# Copy the test.txt file from the local system to the container
COPY test.txt /app/test.txt
 
# Set the default command to display the contents of test.txt
CMD cat /app/test.txt
